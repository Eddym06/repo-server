# AutoQuiz Server - Variables de Entorno
# Copiar este archivo a .env y configurar valores

# ===== SERVIDOR =====
# Puerto del servidor (default: 3000)
PORT=3000

# Host (0.0.0.0 para aceptar conexiones externas, 127.0.0.1 solo local)
HOST=127.0.0.1

# Habilitar logging detallado
ENABLE_LOGGING=true

# Origen permitido adicional (opcional)
# ALLOWED_ORIGIN=https://tudominio.com

# ===== POSTGRESQL DATABASE =====
# Usuario de PostgreSQL
DB_USER=postgres

# Contraseña de PostgreSQL
DB_PASSWORD=a2d27068d014beeadb8f

# Host de PostgreSQL (localhost para Windows local, autoquiz_postgres-autoquiz para servidor)
DB_HOST=autoquiz_postgres-autoquiz

# Puerto de PostgreSQL
DB_PORT=5432

# Nombre de la base de datos
DB_NAME=autoquiz

# SSL (false para local, true para producción)
DB_SSL=false

# ===== MULTI-USER =====
# Máximo de usuarios concurrentes (default: 15)
MAX_CONCURRENT_USERS=15

# Tiempo de expiración de sesiones en ms (default: 3600000 = 1 hora)
SESSION_TIMEOUT=3600000

# Clave de administrador para endpoints /admin/* (opcional)
# ADMIN_KEY=tu-clave-secreta-admin

# ===== RATE LIMITING =====
# Configuración de rate limiting
RATE_LIMIT_COOLDOWN=true
RATE_LIMIT_DEGRADE=true

# Token budget (tokens por minuto)
TOKEN_LIMIT_PER_MINUTE=30000
